DISCORD_WEBHOOK_URL=
DATABASE_SQLITE_PATH=./data/db.sqlite

# generate with 'bunx auth secret --raw'
AUTH_SECRET=

# Strato SMTP. Use the full mailbox address as user and its mailbox password.
AUTH_EMAIL_FROM="the dijker"
AUTH_EMAIL_HOST="smtp.strato.com"
AUTH_EMAIL_PORT=465
AUTH_EMAIL_USER=""
AUTH_EMAIL_PASSWORD=""
